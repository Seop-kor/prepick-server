# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthPayload {
  accessToken: String!
  accessTokenExpiresAt: DateTime!
  refreshToken: String!
  refreshTokenExpiresAt: DateTime!
  user: User!
}

type Category {
  id: ID!
  name: String!
  productIds: [ID!]!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input LoginInput {
  password: String!
  phone: String!
}

type MenuSearchHit {
  id: ID!
  minPrice: Int!
  name: String!
  store: Store!
}

type MenuSearchPage {
  items: [MenuSearchHit!]!
  nextCursor: String
}

type Mutation {
  login(input: LoginInput!): AuthPayload!
  logout(refreshToken: String!): Boolean!
  refreshSession(refreshToken: String!): AuthPayload!
  sendSignupOtp(phone: String!): OtpRequestPayload!
  signUp(input: SignUpInput!): AuthPayload!
  verifySignupOtp(code: String!, phone: String!): OtpVerificationPayload!
}

type OtpRequestPayload {
  expiresAt: DateTime!
  retryAfterSeconds: Int!
}

type OtpVerificationPayload {
  expiresAt: DateTime!
  verificationToken: String!
}

type Product {
  description: String
  id: ID!
  imageUrl: String
  isSoldOut: Boolean!
  minPrice: Int!
  name: String!
  skus: [Sku!]!
  storeId: ID!
}

type Promotion {
  description: String!
  id: ID!
  imageUrl: String
  title: String!
}

type Query {
  currentUser: User!
  healthCheck: Boolean!
  newStores(after: String, first: Int = 20): StorePage!
  product(id: ID!): Product
  promotions: [Promotion!]!
  search(keyword: String!, latitude: Float, longitude: Float): SearchResult!
  store(id: ID!): Store
  storeProducts(storeId: ID!): StoreProducts
  stores(after: String, first: Int = 20, latitude: Float!, longitude: Float!, radiusKm: Float = 5): StorePage!
}

type SearchResult {
  menus(after: String, first: Int = 20): MenuSearchPage!
  stores(after: String, first: Int = 20): StorePage!
}

input SignUpInput {
  name: String!
  password: String!
  verificationToken: String!
}

type Sku {
  id: ID!
  isSoldOut: Boolean!
  name: String!
  price: Int!
}

type Store {
  address: String!
  category: String!
  createdAt: DateTime!
  distanceMeters: Int
  id: ID!
  imageUrl: String
  isOpen: Boolean!
  name: String!
}

type StorePage {
  items: [Store!]!
  nextCursor: String
}

type StoreProducts {
  categories: [Category!]!
  products: [Product!]!
}

type User {
  createdAt: DateTime!
  id: ID!
  name: String!
  phone: String!
}